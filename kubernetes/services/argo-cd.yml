apiVersion: v1
kind: Service
metadata:
  name: argocd-server
  namespace: argocd
spec:
  type: ClusterIP
  selector:
    app: argocd-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80